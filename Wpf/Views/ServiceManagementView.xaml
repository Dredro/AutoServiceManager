<UserControl x:Class="Wpf.Views.Admin.ServiceManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0,0,0,20">
            <TextBlock Text="Panel zarządzania usługami" 
                       FontSize="20" FontWeight="Bold" 
                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Border>

        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Padding="10">
            <ListView Name="ServiceListView">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding Id}" />
                        <GridViewColumn Header="Nazwa" Width="150" DisplayMemberBinding="{Binding Name}" />
                        <GridViewColumn Header="Opis" Width="200" DisplayMemberBinding="{Binding Description}" />
                        <GridViewColumn Header="Cena" Width="100" DisplayMemberBinding="{Binding Price}" />
                        <GridViewColumn Header="Kategoria" Width="120" DisplayMemberBinding="{Binding Category}" />
                        <GridViewColumn Header="Akcje" Width="160">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                        <Button Content="Edytuj" Margin="2" Padding="4"/>
                                        <Button Content="Usuń" Margin="2" Padding="4"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0,20,0,0">
            <Button Content="Dodaj nową usługę" Width="180" Height="30" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
