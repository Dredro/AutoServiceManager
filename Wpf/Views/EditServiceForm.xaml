<UserControl x:Class="Wpf.Views.EditServiceForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:Wpf.ViewModels"      
             xmlns:converters="clr-namespace:Wpf.Core"            
             xmlns:converters1="clr-namespace:Wpf.Converters"      
             xmlns:services="clr-namespace:Wpf.Services"          
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="400">
    
    <UserControl.Resources>
        <converters1:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/> 

        <Style x:Key="StackPanelMarginStyle" TargetType="StackPanel">
            <Setter Property="Margin" Value="0,5,0,10"/>
        </Style>
        <Style x:Key="HorizontalMarginTextbox" TargetType="TextBox">
            <Setter Property="Margin" Value="10,0,10,0"/>
        </Style>
        <Style x:Key="HorizontalMarginLabel" TargetType="Label">
            <Setter Property="Margin" Value="10,0,10,0"/>
        </Style>
    </UserControl.Resources>
    
    <ScrollViewer> 
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>   
                <RowDefinition Height="Auto"/>   
                <RowDefinition Height="3*"/>    
                <RowDefinition Height="Auto"/>   
                <RowDefinition Height="Auto"/>   
                <RowDefinition Height="*"/>      
            </Grid.RowDefinitions>
            
            <Label Content="Edytuj usługę" FontSize="24" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,20"/>

            <StackPanel Grid.Row="1"> 
                <StackPanel Style="{StaticResource StackPanelMarginStyle}">
                    <TextBox 
                        materialDesign:HintAssist.Hint="Nazwa"
                        Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                        Text="{Binding ServiceName, UpdateSourceTrigger=PropertyChanged}"
                        IsEnabled="{Binding IsSaving, Converter={StaticResource InverseBooleanConverter}}"/> 
                </StackPanel>
            </StackPanel>

            <Grid Margin="0,5,0,10" Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBox 
                        materialDesign:HintAssist.Hint="Opis"
                        Grid.Row="0"
                        TextWrapping="Wrap"
                        Margin="0,10,0,10"
                        Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                        Text="{Binding ServiceDescription, UpdateSourceTrigger=PropertyChanged}"
                        IsEnabled="{Binding IsSaving, Converter={StaticResource InverseBooleanConverter}}"/> 
            </Grid>

            <StackPanel Style="{StaticResource StackPanelMarginStyle}"  Grid.Row="3">
                <Label Content="Zakres ceny:" Style="{StaticResource MaterialDesignLabel}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Margin="0,0,0,5" Grid.Column="0" Style="{StaticResource MaterialDesignFloatingHintTextBox}"  materialDesign:HintAssist.Hint="Od"
                             Text="{Binding MinimalPrice, StringFormat={}{0:N2}, UpdateSourceTrigger=PropertyChanged}"
                             IsEnabled="{Binding IsSaving, Converter={StaticResource InverseBooleanConverter}}"/> 
                    <TextBox Margin="0,0,0,5" Grid.Column="1" Style="{StaticResource MaterialDesignFloatingHintTextBox}"  materialDesign:HintAssist.Hint="Do"
                             Text="{Binding MaximalPrice, StringFormat={}{0:N2}, UpdateSourceTrigger=PropertyChanged}"
                             IsEnabled="{Binding IsSaving, Converter={StaticResource InverseBooleanConverter}}"/> 
                </Grid>
            </StackPanel>

            <StackPanel VerticalAlignment="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="4">
                <Button Content="Anuluj" Margin="0,0,10,0" Style="{StaticResource MaterialDesignFlatSecondaryButton}"
                        Command="{Binding CancelCommand}"
                        IsEnabled="{Binding IsSaving, Converter={StaticResource InverseBooleanConverter}}"/> 
                <Button Content="Zapisz" Margin="0,0,10,0" Style="{StaticResource MaterialDesignRaisedButton}"
                        Command="{Binding SaveCommand}"/> 
            </StackPanel>

            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center"
                        Grid.Row="5" Grid.ColumnSpan="2"  
                        Visibility="{Binding IsSaving, Converter={StaticResource BooleanToVisibilityConverter}}">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" IsIndeterminate="True"
                             HorizontalAlignment="Center" Width="40" Height="40" Margin="0,20,0,0"/>
                <TextBlock Text="Zapisywanie..." HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>