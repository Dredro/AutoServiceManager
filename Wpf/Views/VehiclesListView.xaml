<UserControl x:Class="Wpf.Views.VehiclesListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Stretch">
            <TextBlock Text="Pojazdy" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Foreground="White"/>

            <Button Content="Dodaj nowy" Style="{StaticResource MaterialDesignRaisedButton}"
                    HorizontalAlignment="Right" Margin="16,0,0,0"
                    Command="{Binding AddNewVehicleCommand}">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Plus" Width="16" Height="16" Margin="0,0,8,0"/>
                            <TextBlock Text="Dodaj nowy"/>
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>
        </StackPanel>

        <!-- Lista pojazdów -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Vehicles}" AutoGenerateColumns="False" Margin="10"
                  SelectionMode="Single" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding VehicleId}" Width="Auto"/>
                <DataGridTextColumn Header="Marka" Binding="{Binding Make}"/>
                <DataGridTextColumn Header="Model" Binding="{Binding Model}"/>
                <DataGridTextColumn Header="VIN" Binding="{Binding Vin}"/>
                <DataGridTextColumn Header="Nr rej." Binding="{Binding RegistrationNumber}"/>
                <DataGridTextColumn Header="Rok" Binding="{Binding YearOfProduction}"/>
                <DataGridTextColumn Header="Klient" Binding="{Binding Client.PersonalInfo.FullName}"/>
                <DataGridTemplateColumn Header="Akcje">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Style="{StaticResource MaterialDesignFlatButton}" 
                                        Content="{materialDesign:PackIcon Kind=BookOpenBlankVariant}" 
                                        ToolTip="Wyświetl"
                                        Command="{Binding DataContext.ShowVehicleCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>

                                <Button Style="{StaticResource MaterialDesignFlatButton}" 
                                        Content="{materialDesign:PackIcon Kind=Edit}" 
                                        ToolTip="Edytuj"
                                        Command="{Binding DataContext.EditVehicleCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>

                                <Button Style="{StaticResource MaterialDesignFlatButton}" 
                                        Content="{materialDesign:PackIcon Kind=Delete}" 
                                        ToolTip="Usuń"
                                        Command="{Binding DataContext.DeleteVehicleCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>

