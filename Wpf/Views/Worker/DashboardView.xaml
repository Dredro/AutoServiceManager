<UserControl x:Class="Wpf.Views.Worker.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <ScrollViewer Margin="10">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Panel Serwisanta" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" FontSize="25" Margin="10" Foreground="White" />

            <!-- Statystyki -->
            <materialDesign:Card Grid.Row="1" Margin="0,0,0,16">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:Card Margin="8" Width="200"  Background="Gray">
                        <StackPanel Margin="8">
                            <TextBlock Text="Aktywne zlecenia" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Foreground="White"/>
                            <TextBlock Text="{Binding ActiveOrders}" Style="{StaticResource MaterialDesignHeadline4TextBlock}" HorizontalAlignment="Center" Foreground="White"/>
                        </StackPanel>
                    </materialDesign:Card>

                    <materialDesign:Card Margin="8" Width="200"  Background="Gray">
                        <StackPanel Margin="8">
                            <TextBlock Text="Świadczonych usług" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Foreground="White"/>
                            <TextBlock Text="{Binding ServicesProvided}" Style="{StaticResource MaterialDesignHeadline4TextBlock}" HorizontalAlignment="Center" Foreground="White"/>
                        </StackPanel>
                    </materialDesign:Card>

                    <materialDesign:Card Margin="8" Width="200"  Background="Gray">
                        <StackPanel Margin="8">
                            <TextBlock Text="Zlecenia dzisiaj" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Foreground="White"/>
                            <TextBlock Text="{Binding OrdersToday}" Style="{StaticResource MaterialDesignHeadline4TextBlock}" HorizontalAlignment="Center" Foreground="White"/>
                        </StackPanel>
                    </materialDesign:Card>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Row="2" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Twoje aktywne zlecenia" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="8"/>

                    <ItemsControl ItemsSource="{Binding Orders}" Background="DarkGray">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <materialDesign:Card Margin="8" Padding="8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding DescriptionOfWork}" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" FontSize="20"/>
                                            <TextBlock Text="{Binding Customer.Name}" Style="{StaticResource MaterialDesignBody1TextBlock}"/>
                                            <TextBlock Text="{Binding Vehicle.Model}" Style="{StaticResource MaterialDesignBody1TextBlock}"/>
                                            <TextBlock Text="{Binding Status}" Style="{StaticResource MaterialDesignBody1TextBlock}"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="10">
                                            <Button Style="{StaticResource MaterialDesignFloatingActionMiniButton}" Margin="10"
                                                    Content="{materialDesign:PackIcon Kind=Edit}" 
                                                    ToolTip="Edytuj zlecenie"/>
                                            <Button Style="{StaticResource MaterialDesignFloatingActionMiniButton}" Margin="10"
                                                    Content="{materialDesign:PackIcon Kind=Delete}" 
                                                    ToolTip="Usuń zlecenie"/>
                                        </StackPanel>
                                    </Grid>
                                </materialDesign:Card>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Row="3">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Swiadczone usługi" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="8"/>
                        <Button Content="Dodaj nowe" Style="{StaticResource MaterialDesignRaisedButton}"
                                HorizontalAlignment="Right" Margin="10"
                                Command="{Binding AddNewOrderCommand}">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Plus" Width="16" Height="16" Margin="0,0,8,0"/>
                                        <TextBlock Text="Dodaj nowe"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>

                    </StackPanel>
                    <DataGrid ItemsSource="{Binding Services}" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Nazwa" Binding="{Binding ServiceName}"/>
                            <DataGridTextColumn Header="Opis" Binding="{Binding Description}"/>
                            <DataGridTextColumn Header="Cena" Binding="{Binding Price, StringFormat={}{0:C}}"/>
                            <DataGridTextColumn Header="Kategoria" Binding="{Binding Category}"/>
                            <DataGridTemplateColumn Header="Akcje">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Style="{StaticResource MaterialDesignFlatButton}" Content="Edytuj"/>
                                            <Button Style="{StaticResource MaterialDesignFlatButton}" Content="Usuń"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </ScrollViewer>
</UserControl>
