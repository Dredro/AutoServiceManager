<Page x:Class="Wpf.Views.CreateEmployeeForm"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Wpf.Views"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="CreateEmployeeForm">
    <Page.Resources>
        <Style x:Key="StackPanelMarginStyle" TargetType="StackPanel">
            <Setter Property="Margin" Value="0,5,0,10"/>
        </Style>
    </Page.Resources>

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Content="Create Employee" FontSize="24" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1">

            <StackPanel Style="{StaticResource StackPanelMarginStyle}">
                <Label Content="Firstname:" />
                <TextBox/>
            </StackPanel>
            <StackPanel  Style="{StaticResource StackPanelMarginStyle}">
                <Label Content="Lastname:" />
                <TextBox />
            </StackPanel>
            <StackPanel  Style="{StaticResource StackPanelMarginStyle}">
                <Label Content="E-mail:" />
                <TextBox />
            </StackPanel>
            <StackPanel Style="{StaticResource StackPanelMarginStyle}">
                <Label Content="Phone number:" />
                <TextBox />
            </StackPanel>
            <StackPanel Style="{StaticResource StackPanelMarginStyle}">
                <Label Content="Salary:" />
                <TextBox />
            </StackPanel>
            <StackPanel Style="{StaticResource StackPanelMarginStyle}">
                <Label Content="Role:" />
                <!--Room for roles from api-->
                <ComboBox>
                    <ComboBoxItem Content="Mechanic" />
                </ComboBox>
            </StackPanel>
        </StackPanel>
        <StackPanel VerticalAlignment="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2">
            <Button Content="Cancel" Margin="0,0,10,0"/>
            <Button Content="Save" Margin="0,0,10,0"/>
        </StackPanel>
    </Grid>
</Page>
