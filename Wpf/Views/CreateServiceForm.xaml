<Window x:Class="Wpf.Views.CreateServiceForm"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Wpf.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CreateServiceForm">
    <Window.Resources>
        <Style x:Key="StackPanelMarginStyle" TargetType="StackPanel">
            <Setter Property="Margin" Value="0,5,0,10"/>
        </Style>
        <Style x:Key="HorizontalMarginTextbox" TargetType="TextBox">
            <Setter Property="Margin" Value="10,0,10,0"/>
        </Style>
        <Style x:Key="HorizontalMarginLabel" TargetType="Label">
            <Setter Property="Margin" Value="10,0,10,0"/>
        </Style>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Content="Create Service" FontSize="24" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,20"/>

            <StackPanel Style="{StaticResource StackPanelMarginStyle}" Grid.Row="1">
                <Label Content="Name:" />
                <TextBox/>
            </StackPanel>


    
            <Grid Margin="0,5,0,10" Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Label Content="Description:" Grid.Row="0" />
                <TextBox 
        Grid.Row="1"
        TextWrapping="Wrap"
        VerticalAlignment="Stretch"
        Margin="0,10,0,10"/>
            </Grid>


        <StackPanel Style="{StaticResource StackPanelMarginStyle}"  Grid.Row="3">
                <Label Content="Price range:" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="From:" Grid.Column="0" Style="{StaticResource HorizontalMarginLabel}"/>
                    <TextBox Margin="0,0,0,5" Grid.Column="1" Style="{StaticResource HorizontalMarginTextbox}"/>
                    <Label Content="To:" Grid.Column="2" Style="{StaticResource HorizontalMarginLabel}"/>
                    <TextBox Margin="0,0,0,5" Grid.Column="3" Style="{StaticResource HorizontalMarginTextbox}"/>
                </Grid>
            </StackPanel>
        
        <StackPanel VerticalAlignment="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="4">
            <Button Content="Cancel" Margin="0,0,10,0"/>
            <Button Content="Save" Margin="0,0,10,0"/>
        </StackPanel>
    </Grid>
</Window>
