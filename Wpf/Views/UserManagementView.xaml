<UserControl x:Class="Wpf.Views.Admin.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0,0,0,20">
            <TextBlock Text="Panel zarządzania użytkownikami" 
                       FontSize="20" FontWeight="Bold" 
                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Border>

        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Padding="10">
            <ListView Name="UserListView">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Imię" Width="120" DisplayMemberBinding="{Binding FirstName}"/>
                        <GridViewColumn Header="Nazwisko" Width="120" DisplayMemberBinding="{Binding LastName}"/>
                        <GridViewColumn Header="Email" Width="200" DisplayMemberBinding="{Binding Email}"/>
                        <GridViewColumn Header="Akcje" Width="160">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                        <Button Content="Edytuj" Margin="2" Padding="4"/>
                                        <Button Content="Usuń" Margin="2" Padding="4"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0,20,0,0">
            <Button Content="Dodaj nowego" Width="150" Height="30" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
