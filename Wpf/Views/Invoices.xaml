<UserControl x:Class="Wpf.Views.Worker.InvoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="White">

    <ScrollViewer Margin="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Nagłówek -->
            <Border Grid.Row="0" BorderBrush="Gray" BorderThickness="1" Padding="16" Margin="0 0 0 16" CornerRadius="8" Background="White">
                <TextBlock Text="Faktury" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
            </Border>

            <!-- Wybór usługi -->
            <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0 0 0 16" CornerRadius="8" Background="White">
                <ComboBox Width="300" Height="30" HorizontalAlignment="Center">
                    <ComboBoxItem IsSelected="True" IsEnabled="False">Wybierz usługę</ComboBoxItem>
                </ComboBox>
            </Border>

            <!-- Wybór typu faktury -->
            <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0 0 0 16" CornerRadius="8" Background="White">
                <ComboBox Width="300" Height="30" HorizontalAlignment="Center">
                    <ComboBoxItem IsSelected="True" IsEnabled="False">Wybierz typ faktury</ComboBoxItem>
                </ComboBox>
            </Border>

            <!-- Przycisk -->
            <Border Grid.Row="3" BorderBrush="Gray" BorderThickness="1" Padding="10" CornerRadius="8" Background="White">
                <Button Content="Generuj" Width="300" Height="30" HorizontalAlignment="Center" Click="GenerateInvoice_Click"/>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
