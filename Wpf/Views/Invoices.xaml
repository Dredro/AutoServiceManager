<UserControl x:Class="Wpf.Views.Worker.InvoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="White">

    <ScrollViewer Margin = "16" >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height = "*" />
                <RowDefinition Height="Auto"/>
                <RowDefinition Height = "Auto" />
            </Grid.RowDefinitions>


            <!-- Nagłówek -->
                <Border Grid.Row="0" BorderBrush="Gray" BorderThickness="1" Padding="16" Margin="0 0 0 16" CornerRadius="8" Background="White">
                    <TextBlock Text = "Zakończone zamówienia" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                </Border>

                <!-- Wybór usługi -->
                <Border Grid.Row="1" BorderBrush= "Gray" BorderThickness= "1" Padding= "10" Margin= "0 0 0 16" CornerRadius= "8" Background= "White" >
                    <DataGrid Name= "InvoiceDataGrid" AutoGenerateColumns= "False" Margin= "0,0,0,16" >
                    <DataGrid.Columns >
                        <DataGridTextColumn Header= "Id Zlecenia" Width= "*" />
                        <DataGridTextColumn Header= "Data Zakończenia"  Width= "*" />
                        <DataGridTextColumn Header= "Klient"  Width= "*" />
                        <DataGridTextColumn Header= "Pojazd"  Width= "*" />
                        <DataGridTextColumn Header= "Kwota" Width= "*" />
                        <DataGridTemplateColumn Header="Akcje" Width="160">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                        <Button Content="Wyświetl" Margin="2" Padding="4"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                </Border>

                <!--Wybór typu faktury -->
                    <Border Grid.Row= "2" BorderBrush= "Gray" BorderThickness= "1" Padding= "10" Margin= "0 0 0 16" CornerRadius= "8" Background= "White" >
                        <ComboBox Width= "300" Height= "30" HorizontalAlignment= "Center" >
                            <ComboBoxItem IsSelected= "True" IsEnabled= "False" >Wybierz typ potwierdzenia sprzedaży</ComboBoxItem>
                        </ComboBox>
                    </Border>

                    <!-- Przycisk -->
                    <Border Grid.Row= "3" BorderBrush= "Gray" BorderThickness= "1" Padding= "10" CornerRadius= "8" Background= "White" >
                        <Button Content= "Generuj" Width= "300" Height= "30" HorizontalAlignment= "Center" Click= "GenerateInvoice_Click" />
                    </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
