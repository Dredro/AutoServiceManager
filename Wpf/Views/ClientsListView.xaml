<UserControl x:Class="Wpf.Views.ClientsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Stretch">
            <TextBlock Text="Klienci" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Foreground="White"/>

            <Button Content="Dodaj nowego" Style="{StaticResource MaterialDesignRaisedButton}"
                    HorizontalAlignment="Right" Margin="16,0,0,0"
                    Command="{Binding AddNewClientCommand}">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Plus" Width="16" Height="16" Margin="0,0,8,0"/>
                            <TextBlock Text="Dodaj nowego"/>
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>
        </StackPanel>

        <!-- Lista klientów -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Clients}" AutoGenerateColumns="False" Margin="10"
                  SelectionMode="Single" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ClientId}" Width="Auto"/>
                <DataGridTextColumn Header="Imię i nazwisko" Binding="{Binding PersonalInfo.FullName}"/>
                <DataGridTextColumn Header="Email" Binding="{Binding PersonalInfo.Email}"/>
                <DataGridTextColumn Header="Telefon" Binding="{Binding PersonalInfo.Phone}"/>
                <DataGridTemplateColumn Header="Akcje">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Style="{StaticResource MaterialDesignFlatButton}" 
                                        Content="{materialDesign:PackIcon Kind=BookOpenBlankVariant}" 
                                        ToolTip="Wyświetl"
                                        Command="{Binding DataContext.ShowClientCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>

                                <Button Style="{StaticResource MaterialDesignFlatButton}" 
                                        Content="{materialDesign:PackIcon Kind=Edit}" 
                                        ToolTip="Edytuj"
                                        Command="{Binding DataContext.EditClientCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>

                                <Button Style="{StaticResource MaterialDesignFlatButton}" 
                                        Content="{materialDesign:PackIcon Kind=Delete}" 
                                        ToolTip="Usuń"
                                        Command="{Binding DataContext.DeleteClientCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>