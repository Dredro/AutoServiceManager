<Page x:Class="Wpf.Views.StorageManagerDashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Wpf.Views"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="900"
      Title="Panel Magazyniera"
      Background="#F0F2F5">

    <Page.Resources>
        <!-- Style z poprzednich przykładów dla spójności -->
        <Style TargetType="TextBlock" x:Key="PageTitleStyle">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="SectionTitleStyle">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

       

        <Style TargetType="Border" x:Key="SectionBorderStyle">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            
        </Style>

        <Style TargetType="ListBox" x:Key="StyledListBoxStyle">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DDE1E6"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="MinHeight" Value="200"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>

        <Style TargetType="ListBoxItem" x:Key="StyledListBoxItemStyle">
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#EEE"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Name="Bd"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter TargetName="Bd" Property="Background" Value="LightSkyBlue"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="Bd" Property="Background" Value="#E6F2FF"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="true"/>
                                    <Condition Property="Selector.IsSelectionActive" Value="false"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Bd" Property="Background" Value="#D8D8D8"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </MultiTrigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Panel Zarządzania Magazynem" Style="{StaticResource PageTitleStyle}"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,20">
            <Button Content="Dodaj Nową Część" Style="{StaticResource PrimaryButtonStyle}" Click="AddPartButton_Click" />
         
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Kolumna Lista Części -->
            <Border Grid.Column="0" Style="{StaticResource SectionBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Lista Dostępnych Części" Style="{StaticResource SectionTitleStyle}"/>
                    <TextBox Margin="0,0,0,10" Padding="5" Text="Wyszukaj część..." Foreground="Gray" GotFocus="SearchTextBox_GotFocus" LostFocus="SearchTextBox_LostFocus"/>
                    <ListBox x:Name="AvailablePartsListBox"
                 ItemsSource="{Binding AvailableParts}"
                 DisplayMemberPath="Name"
                 Style="{StaticResource StyledListBoxStyle}"
                 ItemContainerStyle="{StaticResource StyledListBoxItemStyle}"
                 SelectionChanged="AvailablePartsListBox_SelectionChanged"/>
                </StackPanel>
            </Border>

            <!-- Kolumna Lista Brakujących Części -->
            <Border Grid.Column="2" Style="{StaticResource SectionBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Lista Brakujących Części" Style="{StaticResource SectionTitleStyle}"/>
                    <TextBox Margin="0,0,0,10" Padding="5" Text="Wyszukaj brakującą część..." Foreground="Gray" GotFocus="SearchTextBox_GotFocus" LostFocus="SearchTextBox_LostFocus"/>
                    <ListBox x:Name="MissingPartsListBox"
                 ItemsSource="{Binding MissingParts}"
                 DisplayMemberPath="Name"
                 Style="{StaticResource StyledListBoxStyle}"
                 ItemContainerStyle="{StaticResource StyledListBoxItemStyle}"
                 SelectionChanged="MissingPartsListBox_SelectionChanged"/>
                    <Button Content="Dodaj Zaznaczone" Style="{StaticResource PrimaryButtonStyle}" Margin="0,15,0,0" HorizontalAlignment="Right" Click="OrderSelectedMissingParts_Click" IsEnabled="False" x:Name="OrderMissingPartsButton"/>
                </StackPanel>
            </Border>

        </Grid>
    </Grid>
</Page>